server {
  listen 80;

  location /nimbleapi/ {
    proxy_pass http://backend:3200/nimbleapi/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
  }

  location / {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /index.html;
  }
}
